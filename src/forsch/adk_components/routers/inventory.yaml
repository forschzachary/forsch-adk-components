# routers inventory — keyword + intention index for all routers
# Frontmatter format is the same as patterns/ — see patterns/inventory.yaml.
# Entries will be populated as you build new routers blocks.

routers:
